<div class="offcanvas-body">
    <div class="minicart-content">
        <div class="minicart-heading">
            <h4 class="mb-0">Shopping Cart</h4>
            <a href="#" class="button-close">
                <i class="pe-7s-close"
                   data-tippy="Close"
                   data-tippy-inertia="true"
                   data-tippy-animation="shift-away"
                   data-tippy-delay="50"
                   data-tippy-arrow="true"
                   data-tippy-theme="sharpborder"></i>
            </a>
        </div>
        <ul class="minicart-list" id="minicart-list">
            @* @foreach (var item in Model.Items)
            {
                <li class="minicart-product">
                    <a class="product-item_remove" href="#">
                        <i class="pe-7s-close"
                           data-tippy="Remove"
                           data-tippy-inertia="true"
                           data-tippy-animation="shift-away"
                           data-tippy-delay="50"
                           data-tippy-arrow="true"
                           data-tippy-theme="sharpborder"></i>
                    </a>
                    <a asp-action="Details" asp-controller="Product" asp-route-id="@item.Id"
                       class="product-item_img">
                        <img class="img-full"
                             src="~/@item.ImageURL"
                             alt="Product Image" />
                    </a>
                    <div class="product-item_content">
                        <a class="product-item_title"
                           asp-action="Details" asp-controller="Product" asp-route-id="@item.Id">@item.Name</a>
                        <span class="product-item_quantity">@item.Count x $@item.Price</span>
                    </div>
                </li>
            } *@
        </ul>
    </div>
    <div class="minicart-item_total">
        <span>Subtotal</span>
        <span class="ammount" id="ammount">@* $@Model.Items.Sum(x=> x.Count * x.Price) *@</span>
    </div>
    <div class="group-btn_wrap d-grid gap-2">
        <a asp-action="Index" asp-controller="Basket" class="btn btn-dark">View Cart</a>
    </div>
</div>